<form method="post" action="{{ path('app_shop_items_inventory_delete', {'id': shop_items_inventory.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ shop_items_inventory.id) }}">
    <button class="btn">Delete</button>
</form>

{% block stylesheets %}
  <link href="https://fonts.googleapis.com/css2?family=EB+Garamond:ital,wght@0,400..800;1,400..800&family=Playfair+Display:ital,wght@0,400..900;1,400..900&display=swap" rel="stylesheet">
{% endblock  %}


<style>
    body{
         font-family: "Playfair Display", serif;
         margin:0;
         padding:0;
         background:#6b6b6b;
         color:#fff;
    }

    a{
        color:white;
    }

    .btn{
        margin-top:20px
    }
</style>